node_modules/
out/
dist/
*.log
test-results/
playwright-report/
build/icon.iconset/

# internal tooling & planning — tracked in GitLab (internal), stripped for public GitHub.
# Machine-local / runtime state stays ignored everywhere:
.superpowers/**/state/
.claude/settings.local.json
.claude/worktrees/

# editor / OS noise
.DS_Store
.vscode/
.idea/

# environment / secrets
.env
.env.*

# local signing credentials (never commit)
build/.notarize.env

# Apple App Store Connect API key (never commit)
build/*.p8
.secrets

# --- für öffentliches GitHub entfernt (nur intern auf GitLab) ---
docs
.claude
.superpowers
AGENTS.md
CLAUDE.md
.gitlab-ci.yml
scripts/publish-to-github.sh
